# The Pyodide runtime files are versioned by folder name, so they can be
# cached "forever"; bumping the version means a new folder (and a new URL).
/pyodide/0.26.4/*
  Cache-Control: public, max-age=31536000, immutable
